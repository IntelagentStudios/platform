{
  "name": "@intelagent/database",
  "version": "1.0.0",
  "private": true,
  "description": "Database schemas and client for Intelagent Platform",
  "main": "./client.js",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "push": "prisma db push",
    "studio": "prisma studio",
    "seed": "node seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  }
}